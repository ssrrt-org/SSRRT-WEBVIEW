{
  "name": "ssrrt",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "start": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "test": "npm run test --prefix frontend",
    "install:all": "npm install --prefix frontend --legacy-peer-deps && npm install --prefix functions",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:firebase": "firebase deploy --only firestore:rules,firestore:indexes,storage,functions"
  }
}
